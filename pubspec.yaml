name: hospital_room_management_system
description: A simple hospital room management system
version: 1.0.0
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  # Your runtime dependencies go here

dev_dependencies:
  test: ^1.25.0
